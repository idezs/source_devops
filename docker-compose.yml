version: '3'

services:
  nginx:
    container_name: nginx
    restart: unless-stopped
    image: nginx:stable-alpine
    volumes:
      - ./static-html:/usr/share/nginx/html
    networks:
      - frontend
    ports:
      - "8080:80"

networks:
  frontend:
    external: true
    name:
        web_network